---
interface Props { rows: { label: string; value: string }[] }
const { rows } = Astro.props;
---
<dl class="simple-example">
  {rows.map(row => <div><dt>{row.label}</dt><dd>{row.value}</dd></div>)}
</dl>
